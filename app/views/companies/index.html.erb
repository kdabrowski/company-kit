<div class="col-md-9" >
  <div class="row">
    <div class="col-md-2">
      <h4>Name<h4>
    </div>
  </div>
  <% @companies.each do |company| %>
    <div class="row">
      <div class="col-md-2">
        <%= link_to company.name, user_company_path(current_user, company) %>
      </div>
    </div>
  <% end %>
  <div class="row mt-1">
    <div class="col-md-2">
    <%= link_to new_user_company_path(current_user) do  %>
      <div class="btn btn-outline-success">Add</div>
    <% end %>
    </div>
  </div>
</div>
